<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/bg"
    tools:context="castillo.apptaxista.login">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ingresa tu numero de "
        android:id="@+id/noTaxi"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="157dp"
        android:textSize="30dp"
        android:textColor="#ff000000"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Taxi"
        android:id="@+id/textView"
        android:textSize="30dp"
        android:textColor="#ff000000"
        android:textStyle="bold"
        android:layout_below="@+id/noTaxi"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:id="@+id/noTx"
        android:editable="false"
        android:layout_marginTop="93dp"
        android:layout_below="@+id/textView"
        android:layout_alignLeft="@+id/buttonEntrar"
        android:layout_alignStart="@+id/buttonEntrar"
        android:layout_alignParentEnd="false"
        android:phoneNumber="false"
        android:inputType="number" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Entrar"
        android:id="@+id/buttonEntrar"
        android:layout_below="@+id/noTx"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="54dp"
        android:textSize="30dp"
        android:onClick="onClickBtnIn" />
</RelativeLayout>
